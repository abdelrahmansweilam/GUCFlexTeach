import 'package:flutter/material.dart';

class ProfileDiscussionsScreen extends StatelessWidget {
  const ProfileDiscussionsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
