import 'package:flutter/material.dart';

class AddAssignmentsScreen extends StatelessWidget {
  const AddAssignmentsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
