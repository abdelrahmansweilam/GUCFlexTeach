import 'package:flutter/material.dart';

class AddDeadlinesScreen extends StatelessWidget {
  const AddDeadlinesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
